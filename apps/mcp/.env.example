# ============================================
# CRAM Books MCP Server - Environment Variables
# ============================================
# Copy this file to .env and fill in your values:
#   cp .env.example .env
#
# NEVER commit .env to git!
# ============================================

# --------------------------------------------
# Google Service Account Credentials (Required)
# --------------------------------------------
# Option 1: JSON content directly (recommended for Railway/production)
# Paste the entire content of your service-account.json file as a single line
GOOGLE_CREDENTIALS_JSON=

# Option 2: Path to JSON file (alternative for local development)
# If set, this takes precedence over GOOGLE_CREDENTIALS_JSON
# GOOGLE_CREDENTIALS_FILE=/path/to/service-account.json

# --------------------------------------------
# Server Configuration
# --------------------------------------------
# Port for the MCP server (Railway sets this automatically)
PORT=8080

# --------------------------------------------
# Legacy: GAS WebApp URL (deprecated, for migration only)
# --------------------------------------------
# EXEC_URL=https://script.google.com/macros/s/YOUR_DEPLOY_ID/exec

# --------------------------------------------
# Optional: HMAC Authentication
# --------------------------------------------
# MCP_HMAC_SECRET=your-secret-key
# MCP_HMAC_REQUIRED=false
